<%= form_for(@serum) do |f| %>
  <% if @serum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@serum.errors.count, "error") %> prohibited this serum from being saved:</h2>

      <ul>
      <% @serum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
