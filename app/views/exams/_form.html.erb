<%= form_for(@exam) do |f| %>
  <% if @exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% @exam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :observations %><br>
    <%= f.text_area :observations %>
  </div>
  <div class="field">
    <%= f.label :archive %><br>
    <%= f.text_area :archive %>
  </div>
  <div class="field">
    <%= f.label :scheduled %><br>
    <%= f.check_box :scheduled %>
  </div>
  <div class="field">
    <%= f.label :medicalrecord_id %><br>
    <%= f.text_field :medicalrecord_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
