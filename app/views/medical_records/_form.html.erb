<%= form_for @record, html: get_default_form_html_options(@record), remote: true do |f| %>
  <div>
    <%= f.text_area :description, class: 'form-control', placeholder: 'Description', autofocus: true, required: true %>
    <%= f.text_area :historic, class: 'form-control', placeholder: 'Historic', required: true %>
    <%= render partial: 'partials/search_button', locals: {form: f, column: :hospitalization_id, placeholder: 'Hospitalization', search_for: 'Hospitalizations', class_to: 'modal_search_button'} %>
  </div>
  <div>
    <%= f.submit action, class: 'btn btn-lg btn-primary btn-block' %>
  </div>
<% end %>